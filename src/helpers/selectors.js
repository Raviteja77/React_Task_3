export const getUserDetails = (state) => state.user.userDetails;
export const getCourses = (state) => state.courses.courses;
export const getAuthors = (state) => state.authors.authors;
export const getCourseAuthors = (state) => state.courseAuthor.authors;
